## Dotfiles
### Linux Installation
Run `setup-linux.sh` with sudo to install the necessary packages.
```bash
sudo ./setup-linux.sh
```
Run `bootstrap-linux.sh` with sudo to copy the configurations.
```bash
sudo ./bootstrap-linux.sh
```